import React, { Component } from "react";
import "./Input.css";

export default function Input() {
  return (
    <input
      type="text"
      className="input"
      placeholder="What needs to be done? "
    />
  );
}
